export const CAR_CONSTANTS = Object.freeze({
  NAME_LENGTH_RANGE: Object.freeze({ min: 1, max: 5 }),
  MIN_MOVE_THRESHOLD: 4,
  MOVE_DISTANCE: 1,
});

export const TRY_CONSTANTS = Object.freeze({
  TRY_RANGE: Object.freeze({ min: 1, max: 200 }),
});

export const SYMBOL = Object.freeze({
  DIVIDE_SYMBOL: ',',
  MOVE_SYMBOL: '-',
  BLANK_SYMBOL: '',
  SPACE: ' ',
});
