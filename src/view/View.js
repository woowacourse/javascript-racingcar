const { input } = require('./InputView');
const { output } = require('./OutputView');
const { close } = require('./Close');

const View = {
  input,

  output,

  close,
};

module.exports = { View };
