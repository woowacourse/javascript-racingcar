const CAR_RULE = Object.freeze({
  initDistance: 1,
  maxLength: 5,
  separator: ',',
});

const GAME_RULE = Object.freeze({
  randomNumberRangeStart: 0,
  randomNumberRangeEnd: 9,
  movingCondition: 4,
});

module.exports = { CAR_RULE, GAME_RULE };
