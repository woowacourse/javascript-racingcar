import deepFreeze from '../../utils/deepFeeze.js';

const RESULT_MESSAGE = deepFreeze({
	result_start: '실행 결과',
	// result_winner: (winner) => `${winner.join(', ')}`,
});

export default RESULT_MESSAGE;
