const SEPERATOR = ',';

const CAR = {
  count: {
    min: 2,
  },
};

const CAR_NAME = {
  range: {
    min: 1,
    max: 5,
  },
};

const CAR_ADVANCE = {
  condition: 4,
  step: 1,
};

const TRY_COUNT = {
  range: {
    min: 1,
  },
};

const RANDOM = {
  range: 10,
};

export default {
  SEPERATOR,
  CAR,
  CAR_NAME,
  CAR_ADVANCE,
  TRY_COUNT,
  RANDOM,
};
