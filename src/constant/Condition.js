const CAR = {
  COUNT: {
    MIN: 2,
  },
};

const CAR_NAME = {
  RANGE: {
    MIN: 1,
    MAX: 5,
  },
};

const CAR_ADVANCE = {
  CONDITION: 4,
  STEP: 1,
};

const TRY_COUNT = {
  RANGE: {
    MIN: 1,
  },
};

const RANDOM = {
  MIN: 0,
  MAX: 9,
};

export default {
  CAR,
  CAR_NAME,
  CAR_ADVANCE,
  TRY_COUNT,
  RANDOM,
};
